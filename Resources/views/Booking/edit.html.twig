{% extends oxygen_layout() %}



{% block oxygen_passbook_title %}
{{ 'oxygen_passbook.booking.edit.section_name'|trans({ '%event%': event.getName() }) }}
{% endblock oxygen_passbook_title %}

{% block oxygen_passbook_content %}
{{ parent() }}
<div>
	<form method="post" {{ form_enctype(form) }}>
	{{ form_errors(form) }}
	{{ form_widget(form) }}
	{{ form_rest(form) }}
	<p>
		<input formnovalidate type="submit" value="{{ 'oxygen_passbook.event.save'|trans }}" class="btn btn-primary" />
	</p>
	</form>
</div>
{% endblock oxygen_passbook_content %}
